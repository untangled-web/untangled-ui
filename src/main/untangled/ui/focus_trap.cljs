(ns untangled.ui.focus-trap
  (:require [om.next :as om :refer [defui]]
            [om.dom :as dom]
            [clojure.string :as str]))

(defui FocusTrap
  Object
  (initLocalState [this] {})

  (componentDidMount [this]
    (let []
      ))

  (componentDidUpdate [this pprops pstate]
    (let []
      ))

  (render [this]
    ))